package common;

// Класс определяет используемую в лабораторной работе функцию для оптимизации
public class LabFormula extends Formula {
    public LabFormula() {
        super(LabFunction.LAB_FUNCTION);
    }
}
